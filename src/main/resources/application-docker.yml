# ============================================================
# Docker 环境配置
# 服务名（mysql / redis）由 docker-compose 的 service name 解析
# ============================================================

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/education_system
    username: root
    password: root123

  data:
    redis:
      host: redis
      port: 6379

  mail:
    host: ${MAIL_HOST:smtp.example.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}

jwt:
  secret: ${JWT_SECRET:education_system_prod_secret_2026_change_me}
  expiration: 86400000
